@model IEnumerable<BikeRentalCompany0.Models.Client>

@{
    ViewData["Title"] = "Index";
}
<div class="container">
<h1>Klienci</h1>

<div class="col-10 text-end">
    <a asp-action="Create"><i class="bi bi-plus-circle-dotted"></i> &nbsp;Dodaj Nowego Klienta</a>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Imie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nazwisko)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NrTelefonu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdKlienta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Imie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwisko)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NrTelefonu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdKlienta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdKlienta">Edytuj</a> |
                <a asp-action="Details" asp-route-id="@item.IdKlienta">Szczegóły</a> |
                <a asp-action="Delete" asp-route-id="@item.IdKlienta">Usuń</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>